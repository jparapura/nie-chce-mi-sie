show dbs
use db-name
show collections
db <- pokazuje nazwę obecnej bazy
db.characters.insertOne({ name: 'Jon Snow' })
db.characters.find()
db.characters.remove({ name: 'Jon snow' })
db.characters.drop()
db.postmessages.find({}, { selectedFile: 0 }) <- znajdź wszystko poza jedną kolumną
